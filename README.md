This project is basically coded in html & css for frontend ,
python for backend and for database we used mongodb server
 which is an unstructured database.
 On the beging the Main page we have provided client to create a 
 campaign with their campaign name,client name,date nd how they
  wanted to start the campaign which is of 3 parts active,completed 
 and paused .
 In the second page alter table the can admin alter,modify,update and delete the 
 campaign in the table page .
 In the next also provided the report page to keep
 track on the campaign and keep on checking the update which
  should be clear and well updated where the campaign tracker keeps 
  tracking the report with the frequent updation of the report as
   it is active,paused or completed time to time,with delete and newly addded.
   -app.py
   -templates{
    -index
    -table
    -report
   }
   -stastic{
    -style
   }

